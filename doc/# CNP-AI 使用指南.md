# CNP-AI 使用指南

在 tmux 里直接使用的智能终端助手：读取当前窗口的各个 pane 内容作为上下文，并在独立的执行 pane 中（经确认后）代你执行命令。

## 目录

- [前置要求](#前置要求)
- [安装](#安装)
- [快速开始](#快速开始)
- [日常使用（窗口/进入/退出）](#日常使用窗口进入退出)
- [工作方式与布局](#工作方式与布局)
- [模式](#模式)
  - [Observe（默认）](#observe默认)
  - [Prepare（更精准的命令跟踪）](#prepare更精准的命令跟踪)
  - [Watch（持续观察）](#watch持续观察)
- [聊天命令（/ 指令）](#聊天命令-指令)
- [命令行用法](#命令行用法)
- [配置](#配置)
  - [配置文件位置与优先级](#配置文件位置与优先级)
  - [常用配置项](#常用配置项)
  - [会话内覆盖（/config）](#会话内覆盖config)
  - [安全确认与白/黑名单](#安全确认与白黑名单)
  - [使用其他模型/服务商](#使用其他模型服务商)
- [MCP（多上下文工具）](#mcp多上下文工具)
- [使用场景与应用价值](#使用场景与应用价值)
- [示例](#示例)
- [故障排查](#故障排查)

---

## 前置要求

- Unix-like 系统（Linux/macOS）
- 可访问安装脚本与模型服务端点

> [!NOTE]
> `tmux` 未安装也可以：默认安装脚本已覆盖并处理 `tmux` 缺失的场景。
>

## 安装

```bash
curl -kfsSL http://cnpai-cnp-bdtest.gwmit.cn/install.sh | bash
```

- 默认安装到 `/usr/local/bin/cnp-ai`
- 默认安装脚本已配置好模型相关信息，默认模型为：`deepseek-v3-2`

## 快速开始

如果尚未安装 CNP-AI，可先执行：

```bash
curl -kfsSL http://cnpai-cnp-bdtest.gwmit.cn/install.sh | bash
```

1) 进入你的环境或者对应机器

```bash
cd /path/to/your/project
```

2) 启动 CNP-AI

```bash
cnp-ai
```

默认安装脚本已经配置好模型相关信息，默认模型为：`deepseek-v3-2`。

3) 在 聊天窗口 输入你的请求，例如：

- `帮我解释这个错误日志并给出修复步骤`

## 日常使用（窗口/进入/退出）

- 窗口切换：使用 tmux 默认前缀键（通常是 `Ctrl-b`）
  - 切换到下一个窗口：`Ctrl-b n`
  - 切换到上一个窗口：`Ctrl-b p`
  - 窗格（pane）之间切换：`Ctrl-b` + 方向键
- 调整窗格大小（比例）：
  - `Ctrl-b` 后按住 `Ctrl` 键不放，同时按 `方向键`（上下）可调整当前窗格大小（部分终端可能需要配置）
- 查看屏幕外文字（历史回滚）：
  - 按 `Ctrl-b [` 进入复制模式（Copy Mode）
  - 此时右上角通常会显示行数提示，使用 `方向键`、`PageUp/PageDown` 即可滚动查看历史输出
  - 按 `q` 退出该模式，回到正常命令行输入状态
- 进入项目：建议在目标项目目录下启动 `cnp-ai`，并把与项目相关的命令输出留在同一窗口内，便于 CNP-AI 读取上下文
- 退出项目：在 Chat Pane 输入 `/exit` 结束本次会话（如需彻底结束窗口，可再按 tmux 的窗口关闭快捷键）

## 工作方式与布局

CNP-AI 以“一个 tmux 窗口一个实例”的方式工作，并将当前窗口的 pane 分为两类：

- Chat Pane：你与 AI 交互的地方
- Exec Pane：AI 建议并经确认后实际执行命令的 pane

## 聊天命令（/ 指令）

| 指令 | 说明 |
| --- | --- |
| `/clear` | 清空聊天历史并清空 Chat Pane 显示 |
| `/reset` | 清空聊天历史并清空 Chat Pane + Exec Pane 显示 |
| `/exit` | 退出 

## 使用场景与应用价值

- 多 pane 上下文协作：同时阅读「构建输出/日志/编辑器/运行态」等多个窗格内容，减少复制粘贴上下文的成本
- 排障与定位：基于当前窗口可见信息快速给出根因假设、验证步骤与最小修复建议
- 安全的命令辅助：在白/黑名单与确认机制约束下，生成可执行方案并在独立 Exec Pane 中执行，降低误操作风险
- 提效与学习：将你的命令行操作当作“现场”，给出更高效/更稳妥的替代命令与解释，形成可复用的操作范式

## 示例

- 根据pane 的上下文做排障：

